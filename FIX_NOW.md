# ⚡ إصلاح سريع - الصفحة البيضاء

## 🎯 الحل في 3 أوامر

```bash
# 1. شغّل السكريبت
chmod +x fix-white-page.sh
./fix-white-page.sh

# 2. ارفع على GitHub
git add .
git commit -m "Fix white page"
git push origin main

# 3. تسنّى 2 دقائق وافتح الموقع
```

---

## 📋 واش ما شغلش السكريبت؟ جرب هادشي يدوياً:

### الطريقة 1: تأكد من اسم الريبو

```bash
# شوف اسم الريبو ديالك
git remote get-url origin

# غادي يبان ليك حاجة بحال:
# https://github.com/USERNAME/اسم-الريبو.git
```

خود **اسم-الريبو** (بدون .git)

---

### الطريقة 2: صحح `vite.config.ts`

افتح الملف وبدل السطر 9:

```typescript
base: '/اسم-الريبو-ديالك/',
```

**مثال:**
- إلا الريبو سميتو `graphitube-app` → `base: '/graphitube-app/',`
- إلا الريبو سميتو `Graphitube` → `base: '/Graphitube/',`

---

### الطريقة 3: بني وارفع

```bash
# نضّف
rm -rf dist .vite node_modules/.vite

# بني
npm run build

# تأكد
ls dist/index.html

# ارفع
git add .
git commit -m "Fix base path"
git push origin main
```

---

## 🔍 كيفاش نتأكد من الحل؟

بعد ما ترفع، دوز لـ GitHub:

1. اضغط على **Actions** (فوق)
2. شوف آخر workflow كيخدم
3. تسنّى يولّي أخضر ✅
4. دوز لـ: `https://USERNAME.github.io/اسم-الريبو/`

---

## ❌ مازال ماخدامش؟

### تأكد من Settings

1. دوز لـ الريبو → **Settings**
2. من الجانب اليسار → **Pages**
3. تحت **Source**:
   - إلا اخترتي **GitHub Actions** → الحل مزيان
   - إلا اخترتي **Deploy from a branch** → بدلها لـ **GitHub Actions**

---

## 🆘 تشخيص سريع

افتح الموقع واضغط **F12** → **Console**

### شوف واش عندك:

#### 1. خطأ: `404 Not Found: /assets/index-xyz.js`

**السبب:** base path غالط

**الحل:**
```bash
# شوف اسم الريبو
git remote -v

# بدل base في vite.config.ts
# ارفع من جديد
```

#### 2. خطأ: `Failed to load module script`

**السبب:** الملفات ماكيحملوش

**الحل:**
```bash
rm -rf dist
npm run build
git add dist -f  # إلا كنتي كتنشر dist
git push
```

#### 3. خطأ: `Uncaught SyntaxError`

**السبب:** كيرد HTML بدل JavaScript

**الحل:**
```bash
# تأكد من base path
# امسح الكاش ديال البراوزر
# Ctrl + Shift + R
```

---

## 💡 الحل الأسهل على الإطلاق

**غيّر اسم الريبو لـ `Graphitube` بالضبط:**

1. دوز لـ الريبو على GitHub
2. **Settings** → فوق فـ **Repository name**
3. بدلو لـ: `Graphitube`
4. **Rename**

وبعدها الكود غادي يخدم بدون تعديل! 🎉

---

## 📞 خلاصة

**المشكلة الرئيسية:** عدم تطابق اسم الريبو مع `base` في `vite.config.ts`

**الحل الأسرع:**
1. شوف اسم الريبو: `git remote -v`
2. بدل `base` في `vite.config.ts`
3. `npm run build`
4. `git push`

**أو:**
- غيّر اسم الريبو لـ `Graphitube` بالضبط

---

## ✅ علامات النجاح

فاش يخدم مزيان:

- ✅ الصفحة كتحمّل (ماشي بيضاء)
- ✅ Console فيه 0 أخطاء
- ✅ كتشوف Logo و Content
- ✅ الروابط كيخدمو

---

**للمزيد من التفاصيل:** [حل_الصفحة_البيضاء.md](./حل_الصفحة_البيضاء.md)

**بالتوفيق! 🚀**
